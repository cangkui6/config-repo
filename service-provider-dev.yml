server:
  port: 8081

spring:
  application:
    name: service-provider

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: service-provider-8081
provider:
  message: "这是修改后的服务提供者消息 - 时间戳：[当前时间]"